name: Build Android APK (TWA)

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows you to run this manually from the Actions tab

jobs:
  build:
    name: Build APK using Bubblewrap
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Bubblewrap CLI
        run: npm install -g @bubblewrap/cli

      - name: Configure Bubblewrap
        run: |
          mkdir -p ~/.bubblewrap
          # Provide the paths for Android SDK and JDK that GitHub Actions provides by default
          cat << EOF > ~/.bubblewrap/config.json
          {
            "jdkPath": "${JAVA_HOME}",
            "androidSdkPath": "${ANDROID_HOME}"
          }
          EOF

      - name: Initialize Android Project
        run: |
          # Use a default password to prevent infinite prompt loops when Bubblewrap asks for keystore details
          yes "password123" | bubblewrap init --manifest="https://sdcinestream.qzz.io/manifest.json" --directory=./android-app

      - name: Build APK
        working-directory: ./android-app
        run: |
          # Builds the unsigned and signed APKs, automatically answering the keystore password prompt
          yes "password123" | bubblewrap build

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SDCineStream-App
          path: |
            ./android-app/app-release-signed.apk
            ./android-app/app-release-unsigned.apk
